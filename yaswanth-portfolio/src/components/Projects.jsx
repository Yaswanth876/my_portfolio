import { useState } from 'react'
import { motion, AnimatePresence } from 'framer-motion'
import { Github, ArrowUpRight } from 'lucide-react'
import { projects } from '../data/projects'
const filters=['All','AI/ML','Full Stack']
export default function Projects(){const [filter,setFilter]=useState('All'); const shown=filter==='All'?projects:projects.filter(p=>p.category===filter); return <section id="projects" className="section page-container"><div className="mb-8"><p className="section-label">04 / Selected Work</p><h2 className="section-title">Ideas made tangible.</h2><p className="section-copy">A selection of AI/ML and full-stack work grounded in real problems and practical implementation.</p></div><div className="project-filter mb-6">{filters.map(item=><button onClick={()=>setFilter(item)} className={`filter-button ${filter===item?'active':''}`} key={item}>{item}</button>)}</div><AnimatePresence mode="wait"><motion.div key={filter} initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} exit={{opacity:0}} className="project-grid">{shown.map((p,index)=><motion.article initial={{opacity:0,y:15}} animate={{opacity:1,y:0}} transition={{delay:index*.05}} key={p.id} className="project-card surface"><div className="flex items-center justify-between"><span className="section-label">{p.category}</span><span className="mono text-xs text-[var(--muted)]">0{p.id}</span></div><h3>{p.title}</h3><p>{p.description}</p><div className="mt-4">{p.tech.map(t=><span className="chip" key={t}>{t}</span>)}</div><div className="project-footer"><a className="text-link" href={p.github} target="_blank" rel="noreferrer"><Github size={14}/> Source</a><a className="text-link" href={p.live} target="_blank" rel="noreferrer">View project <ArrowUpRight size={14}/></a></div></motion.article>)}</motion.div></AnimatePresence></section>}
